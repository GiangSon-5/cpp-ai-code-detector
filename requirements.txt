# ==============================================================================
# C++ AI Code Detector — Master Requirements (Local Environment)
# ==============================================================================
# Core Web Framework (Local)
Django>=5.0,<6.0
djangorestframework>=3.15
django-environ>=0.11
django-cors-headers>=4.3
psycopg2-binary>=2.9

# Async AI Service (FastAPI — may also run locally for dev)
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
httpx>=0.27

# AI / ML Core (Lightweight for local)
pandas>=2.2
lizard>=1.6

# LangGraph Agent
langgraph>=0.0.40
langchain-core>=0.2

# Background Tasks
celery>=5.3
redis>=5.0

# Message Broker (Kafka-compatible client for Redpanda)
confluent-kafka>=2.3

# Data Lake / S3
boto3>=1.34
dagshub>=0.3

# Monitoring helpers
prometheus-client>=0.20

# Tunneling (Colab)
pyngrok>=7.1

# Logging / Utilities
python-dotenv>=1.0
orjson>=3.10

# Dev / Testing
pytest>=8.1
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27
